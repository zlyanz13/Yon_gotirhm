#include <string>
#include <vector>

using namespace std;

struct cord
{
    int x;
    int y;
};

vector<struct cord *> que;

int solution(vector<int> food_times, long long k)
{
    int answer = 0;
    return answer;
}